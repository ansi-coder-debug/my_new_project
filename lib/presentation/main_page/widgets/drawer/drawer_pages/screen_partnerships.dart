import 'package:flutter/material.dart';

class ScreenPartnerships extends StatelessWidget {
  const ScreenPartnerships({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}